<UserControl x:Class="CDriveMaster.UI.Views.WeChatCleanupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="扫描微信数据"
                    Command="{Binding ScanCommand}"
                    Width="120"
                    Height="32"
                    Margin="0,0,10,0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="IsEnabled" Value="True" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                <Setter Property="IsEnabled" Value="False" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" />
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding BucketResults}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="类别名称" Binding="{Binding Bucket.Description}" Width="*" />
                <DataGridTextColumn Header="风险等级" Binding="{Binding Bucket.RiskLevel}" Width="120" />
                <DataGridTextColumn Header="预计释放(字节)" Binding="{Binding Bucket.EstimatedSizeBytes}" Width="160" />
                <DataGridTextColumn Header="分析状态" Binding="{Binding FinalStatus}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
