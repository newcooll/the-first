<UserControl x:Class="CDriveMaster.UI.Views.HelpManualView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Border Padding="28"
            Background="#FFF8FAFC"
            BorderBrush="#FFD7E2EE"
            BorderThickness="1"
            CornerRadius="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding Title}"
                       FontSize="30"
                       FontWeight="Bold"
                       Foreground="#FF0F172A" />

            <TextBlock Grid.Row="1"
                       Margin="0,8,0,18"
                       Text="{Binding Subtitle}"
                       Foreground="#FF475569"
                       TextWrapping="Wrap" />

            <Border Grid.Row="2"
                    Padding="18"
                    Background="White"
                    BorderBrush="#FFE2E8F0"
                    BorderThickness="1"
                    CornerRadius="14">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBox Text="{Binding ManualText}"
                             IsReadOnly="True"
                             BorderThickness="0"
                             Background="Transparent"
                             Foreground="#FF1E293B"
                             FontFamily="Microsoft YaHei UI"
                             FontSize="14"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Disabled"
                             HorizontalScrollBarVisibility="Disabled" />
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</UserControl>
